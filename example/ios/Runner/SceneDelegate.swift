import Flutter
import UIKit

/// Required by `Info.plist`, which names this class in
/// `UIApplicationSceneManifest` as `$(PRODUCT_MODULE_NAME).SceneDelegate`.
/// The scene lifecycle is handled entirely by `FlutterSceneDelegate`, so the
/// body is intentionally empty — removing the class breaks app launch.
class SceneDelegate: FlutterSceneDelegate { // NOSONAR

}
